## To start the project 
 1. clone the repository
 2. open a terminal and reun `cd backend`
 3. then run `node --no-warnings=ExperimentalWarning --loader ts-node/esm src/app.ts`
 4. open another terminal and run `cd frontend`
 5. then run  `npm start`
